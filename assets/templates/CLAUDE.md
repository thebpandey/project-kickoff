<!--
TEMPLATE INSTRUCTIONS — remove this comment after adapting the document.
Keep this file short. Shared policy belongs in AGENTS.md.
-->

# Claude Code adapter for {{Project name}}

Adapter baseline: project-kickoff {{semantic version}}; {{approval/version pointer}}.

@AGENTS.md

Before any project work, read `AGENTS.md` in the repository root and follow it as
the common project policy. If the `@AGENTS.md` import is unavailable, unresolved,
or omitted from the loaded context, open and read that file directly with the
available file-reading tool before proceeding.

Use only Claude Code tools, agents, models, and effort controls that are actually
available in this session. Record the actual host capability used in handoffs.
Do not duplicate or weaken the common policy here. If this adapter conflicts
with `AGENTS.md` at the same instruction level, `AGENTS.md` controls.

Project-specific Claude Code difference: {{None, or the minimal approved host
difference that cannot live in AGENTS.md}}.
